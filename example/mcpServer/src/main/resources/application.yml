server:
  port: 8081

spring:
  ai:
    mcp:
      server:
        name: jsonbom-mcp-server
        version: 1.0.0
        protocol: STATELESS
        type: ASYNC
        stateless:
          mcp-endpint:/mcp
